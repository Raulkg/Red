import React from "react";
import { jsx } from "@emotion/core";
import styled from "@emotion/styled";
import { ThemeProvider, useTheme } from "emotion-theming";

export const MatchRate = ({ text }) => {
  return <div> {text}</div>;
};
